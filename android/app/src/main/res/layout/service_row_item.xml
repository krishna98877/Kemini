<?xml version="1.0" encoding="utf-8"?>
<!-- Reusable row for a single service in the connectors panel -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/service_row"
    android:layout_width="match_parent"
    android:layout_height="52dp"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingStart="4dp"
    android:paddingEnd="4dp"
    android:layout_marginBottom="2dp"
    android:background="@android:color/transparent">

    <!-- Service icon (colored circle with letter) -->
    <FrameLayout
        android:id="@+id/service_icon_frame"
        android:layout_width="34dp"
        android:layout_height="34dp">

        <TextView
            android:id="@+id/service_icon_letter"
            android:layout_width="34dp"
            android:layout_height="34dp"
            android:gravity="center"
            android:textSize="15sp"
            android:textStyle="bold"
            android:textColor="#FFFFFF"
            android:background="@drawable/circle_bg_black" />
    </FrameLayout>

    <!-- Service name -->
    <TextView
        android:id="@+id/service_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:textColor="#FFFFFF"
        android:textSize="14sp"
        android:layout_marginStart="12dp" />

    <!-- Status indicator -->
    <View
        android:id="@+id/service_status_dot"
        android:layout_width="8dp"
        android:layout_height="8dp"
        android:layout_marginEnd="10dp"
        android:visibility="gone" />

    <!-- Connect / Disconnect button -->
    <TextView
        android:id="@+id/service_action_btn"
        android:layout_width="wrap_content"
        android:layout_height="28dp"
        android:gravity="center"
        android:paddingStart="14dp"
        android:paddingEnd="14dp"
        android:text="Connect"
        android:textColor="#00F6FF"
        android:textSize="11sp"
        android:textStyle="bold"
        android:background="@drawable/glass_input_bg" />

</LinearLayout>